# Copy to terraform.auto.tfvars (gitignored) and fill in.
name                = "platform"
environment         = "production"
allowed_admin_cidrs = ["203.0.113.0/24"] # your admin network, never 0.0.0.0/0
ssh_public_key      = "ssh-ed25519 AAAA... admin@example"
